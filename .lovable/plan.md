## Plan

Add `check-24.cc` to the `allowedHosts` array in `vite.config.ts`.

### Änderung

In `vite.config.ts`, `server.allowedHosts` erweitern:

```ts
allowedHosts: [
  "klimabonus-offiziell.cc",
  "antrag-klimabonus.cc",
  "voba-investment.net",
  "www.voba-investment.net",
  "check-24.cc",
],
```

Keine weiteren Änderungen.